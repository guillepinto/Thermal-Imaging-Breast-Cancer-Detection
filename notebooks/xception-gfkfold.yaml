# method can be grid, random or bayes
method: grid

# the best hyperparameters found
parameters:
  architecture:
    values:
      - xception
  augmented:
    values:
      - false
      - true
  batch_size:
    values:
      - 16
  crop:
    values:
      - true
  learning_rate:
    values: 
      - 0.005042
  normalize:
    values:
      - false
  optimizer:
    values:
      - sgd

# the program to run
program: train_gkfold.py
project: dip-project
